# Decisions Ledger — Holmes

Decisions reserved for the human. Format: `D-## | Decision required | Context / options | Status (OPEN / DECIDED: <answer, date>)`

Agents must never resolve a D-item. A decision is DECIDED only when the human records it here (or says so explicitly and an agent records it verbatim).

**D-01** | **Which license governs Holmes?** | Current LICENSE is Apache-2.0 (F-001). The *generic* audit template's gate requires AGPL-3.0-or-later or GPL-3.0 — but `docs/holmes-spec-v2.md` §7 Phase 0 specifies an "Independent **Apache-2.0-compatible** repo" (goose itself is Apache-2.0), and the constitution in the orientation doc lists no copyleft gate. Options: (a) keep Apache-2.0 as the spec implies — the existing LICENSE stands; (b) AGPL-3.0-or-later — copyleft posture, still compatible with Apache-2.0 *dependencies*; (c) GPL-3.0. Note "Apache-2.0-compatible" is ambiguous between (a) and (b); only you can say which was meant. Decide before any code or outside contribution lands. | **DECIDED: AGPL-3.0-or-later — Martin, 2026-07-18, in-band ratification at Gate Zero (b)**
> **Evidence (read from disk 2026-07-18, `Alfred` clone `1801bc3`):** `../Alfred/LICENSE` identifying lines, quoted verbatim: "GNU AFFERO GENERAL PUBLIC LICENSE" / "Version 3, 19 November 2007" — the matching-Alfred premise holds. Repo `LICENSE` swapped to a **byte-identical copy of `Alfred/LICENSE`** (sha256 `d8a6cc31abc16b6748c7a21f21611f5a1ec33f67d22ca23d7da1c19b95496bee`; standard AGPL-3.0 structure verified: TERMS AND CONDITIONS L26, END OF TERMS L214, How-to-Apply L216). gnu.org byte-level cross-check UNVERIFIED-live (egress blocked, CONNECT 403); the copy is a line-wrapping variant of the canonical text. "Or-later" is recorded in README/notices per GNU convention — the LICENSE file itself carries the v3 text. Resolves F-001. Lands on `main` via the session PR only.

**D-02** | **Deploy target?** | Blueprint implies self-hosted/local-first but never states the surface (F-002, F-007). Options include: local desktop daemon alongside GooseClaw; self-hosted server; hybrid. Firecracker requires Linux/KVM — this constrains laptop-local designs on macOS/Windows and must inform the choice. | OPEN — **largely answered by spec v2 §4.1** (Tauri 2 + SolidJS desktop, goose over ACP, self-hosted Graphiti). Residual question, per spec §8: sandbox choice per workload/OS (Firecracker vs V8-isolate path) — decide at Phase 3.

**D-03** | **Runtime architecture: what is Goose to Holmes?** | The decks contradict each other (F-004): Holmes "built on Goose" vs. Goose as "The Hands" belonging to Alfred/the triad vs. GooseClaw as a separately-named runtime. Decide the substrate and the component boundaries before the engineering spec. | OPEN — **spec v2 §4.1/§5 answers this** (Holmes and Alfred are siblings on goose over Zed's ACP; decks superseded). Recommend closing as DECIDED-BY-SPEC; needs your confirmation, not mine.

**D-04** | **Audit runner and venue** | The charter assumes a claude.ai project with `docs/case-file/` as KB, phase-gated with GO replies. Confirm venue and who drives (you paste-and-GO, or an agent session runs it end to end with stops at each phase). Context added 2026-07-13: a v1→v2 QA pass already happened (folded into spec v2), and the orientation doc defines the operating loop (Claude Code builds + emits readouts → claude.ai project pressure-tests). The fresh adversarial audit may be redundant with that loop, or valuable as a pre-Phase-0 gate — your call. | OPEN
> Cross-ref 2026-07-18: D-06 records the 2026-07-13 second-pass audit as the charter's verdict. Whether a fresh adversarial audit still runs (and its venue/driver) remains this item's open question — not resolved by D-06.

**D-05** | **Phase 0 smoke-test mode** | From `docs/holmes-project-orientation.md` §6 open threads: cloud Tier-1 key vs. fully offline Tier-2 via Ollama for the Phase 0 smoke test. Blocks running the Phase 0 kickoff. | **DECIDED: Tier-1 cloud — Martin, 2026-07-18, in-band** ("Smoke-test mode is Tier-1 cloud"; "Key is set")
> Residual 2026-07-18: no provider key is visible inside the current session container (environment secrets inject at container start; full env-name sweep found no `ANTHROPIC_API_KEY`/`GOOGLE_API_KEY`). Provider "confirmed responding" (pre-flight item 6) remains UNVERIFIED this session; re-verify at next container start. No round-trip faked.

**D-06** | **Charter verdict — what satisfies "the adversarial audit produces a READY verdict"?** | Loop §3.0(a): record the 2026-07-13 second-pass audit (verdict: SHIP WITH FIXES; fixes applied in loop v2 as amendment queue A1–A11) as the charter's verdict. Note: the audit report file itself is not yet in `docs/audit/` (staging obligation — human supplies the file; never reconstructed here). | **DECIDED: ACCEPTED as the charter's verdict — Martin, 2026-07-18, in-band** ("Audit accepted: YES")
> **Staging obligation satisfied 2026-07-18 (later):** the report landed on `main` as `docs/audit/holmes-master-build-loop-v1-second-pass-audit.md`. Spot-verified on disk: "**SHIP WITH FIXES**" verdict (§2, L21), amendment queue **A1–A11** (§L79–L89), auditor pass date 2026-07-13. The provenance doc's UNVERIFIED tag on the verdict is lifted.

**D-07** | **Git reconciliation — session-branch restart after PR #3 partial merge** | PR #3 merged through `1b56593` only; unmerged `3ce2524` re-applied verbatim as `47b000d` (ledger files diff-empty vs `3ce2524`, verified); remote session branch updated by one `--force-with-lease` push; `origin/main` untouched — 16-commit superset verified at pre-flight, no rewrite. All landings to `main` remain PR-on-explicit-go-ahead. | **DECIDED: APPROVED — Martin, 2026-07-18, in-band** ("force-with-lease push — APPROVED"); executed same session

**D-08** | **Loop discipline ratification (Gate Zero d)** | Working-branch writes autonomous under /loop; `main` and all irreversibles Rule-9-gated (loop §3.0(d)). | **DECIDED: CONFIRMED — Martin, 2026-07-18, in-band** ("Loop discipline: CONFIRMED")

**D-09** | **The Wall backend (Phase 2)** | Graphiti stores in Neo4j or FalkorDB (spec §3.1/§4.4; Kuzu abandoned). FalkorDB is **SSPL-licensed** — a source-available, non-OSI license the FSF/OSI treat as non-free; distributing an AGPL-3.0-or-later work that requires an SSPL service is incompatible with Holmes's license posture (D-01). Neo4j Community Edition is **GPLv3** (VERIFIED 2026-07-19, primary: `github.com/neo4j/neo4j` LICENSE + `neo4j.com/legal-terms` + issue #8331), same copyleft family as AGPL and run as a *separate service/aggregate* (not linked), so compatible. | **DECIDED: Neo4j Community Edition — Martin, 2026-07-19, in-band.** FalkorDB rejected (SSPL, AGPL-incompatible for distribution). Holmes's own license unchanged: AGPL-3.0-or-later per D-01.

**D-10** | **Review model — who reviews what, when** | Holmes routes case files to human review (spec §2 Phase 6, handoff-only); the loop needs the review topology stated so "the block" (constitution #12) is not vaguely a central per-request channel. | **DECIDED: three-layer, local-first — Martin, 2026-07-19, in-band.** (1) **Build-time review = Martin** — PRs, phase gates, D-items; ends at ship. (2) **Run-time review = each operator reviews their own cases on their own machine** — handoff-only resolution; Holmes never acts on a verdict. (3) **Pattern-time accountability = the Phase 5 block layer** — community-level, never per-request. **No central per-request review channel exists, by design** (local-first sovereignty); do not build one.

**D-11** | **API-push authorization — may the agent push via the GitHub API (MCP), vs `git push`?** | The question was **asked four times and never answered** (recorded 2026-07-19). Until the human ratifies it, treat API-push as **not authorized**; use `git push` on the session branch (which works directly) and never route a landing through the API to bypass a `git push` guard. **Standing rule (Martin, 2026-07-19): a stop the agent declares binds the agent until the human answers** — the agent does not self-clear its own stop. | **OPEN — recorded UNRATIFIED, 2026-07-19.** Agents must not resolve this.

**D-12** | **Graphiti adoption vs the vendor gate — how to get a temporal graph without `openai` in the dependency tree?** | `graphiti-core` 0.29.2 (the pinned version; supports Neo4j) **base-depends on `openai>=1.91.0` and `posthog`** (VERIFIED 2026-07-19, primary: PyPI `requires_dist`). `openai` in the Python tree violates constitution #5 / AC-DL-2 (a Blocker, distinct from the runtime-traffic concern the spec's "pin permitted clients" solves); `posthog` is a phone-home telemetry SDK vs constitution #9. Graphiti's client layer **is** pluggable at runtime (anthropic / google-genai / groq clients ship as extras), so the *runtime* denylist is satisfiable — only the *base package's* unconditional `openai` import and `posthog` are the problem. Options: **(a)** vendor an **AGPL fork of graphiti-core** that moves `openai` to an optional extra and drops `posthog` (ongoing upstream-sync burden; keeps Graphiti's engine); **(b)** **reimplement the needed temporal-graph subset** (bi-temporal edges, invalidation-not-deletion, provenance) directly against the Neo4j Rust/Bolt driver — permitted-only tree, full control, more code; **(c)** upstream a PR making `openai`/`posthog` optional and wait (schedule risk). See F-027. | **DECIDED: reimplement the subset — Martin, 2026-07-19, in-band ruling with riders.** (a) **Graphiti dropped** (F-027 blocker: base-deps `openai` + `posthog`); The Wall builds on the Neo4j Bolt driver + permitted clients only. (b) Spec amendment queued upstream per the sync rule — the Graphiti section revises to the owned subset (**A-08**). (c) AC-DL-1 §6 **generalizes** and still lands at 2b: memory layer with no explicit LLM config reaches no excluded endpoint, proven by test. (d) Evaluate the Rust Bolt driver (`neo4rs`) first — if it satisfies 2a/2c, the Python surface drops to zero; call reported with evidence. (e) File the upstream PR making `openai`/`posthog` optional extras — no schedule dependency on it (draft in `docs/upstream/`; external-repo access is outside this session's GitHub scope, so Martin files it).
