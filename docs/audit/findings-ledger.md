# Findings Ledger — Holmes

Format: `F-### | Severity | Category | Location | Evidence (quote or ABSENT) | Why it matters | Recommended fix | Confidence (H/M/L)`

IDs are stable. The audit proper (Phases 0–7) appends here; it may supersede but must not renumber the pre-audit findings below.

## Pre-audit findings (logged 2026-07-13, at case-file creation)

**F-001** | BLOCKER→CONTESTED | licensing | `LICENSE` | Repo LICENSE is Apache License Version 2.0 (verified by reading the file 2026-07-13) | Violates the standing licensing gate (AGPL-3.0-or-later or GPL-3.0). Cheap to fix now, expensive after outside contributions arrive — every contributor added under Apache-2.0 complicates relicensing | Human decision D-01: relicense before any code lands | H
> **Status update 2026-07-13 (later session):** `docs/holmes-spec-v2.md` §7 Phase 0 specifies an "Independent **Apache-2.0-compatible** repo," and the constitution in `docs/holmes-project-orientation.md` §3 lists no AGPL/GPL gate. The AGPL/GPL gate came from the *generic* audit template. Which instrument governs Holmes's license is now squarely D-01 — the Apache-2.0 LICENSE may be intentional. Severity pending human ruling.
> **Status update 2026-07-18: RESOLVED by D-01 ratification.** Martin ratified **AGPL-3.0-or-later** in-band at Gate Zero (b), with `Alfred/LICENSE` quoted from disk as evidence (see D-01). `LICENSE` swapped on the session branch to a byte-identical copy of `Alfred/LICENSE` (sha256 `d8a6cc31…96bee`); README updated. Lands on `main` via the session PR — the finding closes fully on merge.

**F-002** | MAJOR | spec | `docs/case-file/01-blueprint-kb.md` §"What the blueprint does NOT contain" | ABSENT — blueprint names Goose, Graphiti, and Firecracker but contains no versions, APIs, data models, deployment target, resource budgets, or composition design | The three named technologies have real integration constraints (Graphiti's backend requirements, Firecracker's Linux/KVM requirement) that may contradict the local-first laptop story; nobody has checked | Engineering spec must precede build; audit Phase 3 forces it | H
> **Status update 2026-07-13 (later session): LARGELY RESOLVED.** `docs/holmes-spec-v2.md` (landed this session, previously local-only) is the engineering spec: goose/ACP substrate with sources, Graphiti (Neo4j/FalkorDB) for "The Wall," Firecracker (E2B OSS) sandbox baseline, two-tier model strategy with pricing, supply-chain CI stack, Tauri 2 + SolidJS shell. Remaining gaps: test plan, deploy runbook, data-model detail — see F-007.

**F-003** | MAJOR | process | Drive + GitHub (see `docs/case-file/00-provenance.md` §"Negative results") | ABSENT — no build plan, milestones, kickoff prompt, or acceptance criteria exist for Holmes anywhere | Build cannot start without a plan; the blueprint's six phases are runtime workflow, not construction sequence, and conflating them would misorder the build | Run audit through Phase 6, which produces the kickoff prompt; interim proposal in `docs/roadmap/build-phases.md` | H
> **Status update 2026-07-13 (later session): RESOLVED — finding was scoped to Drive+GitHub and was accurate there, but the artifacts existed locally.** `docs/holmes-spec-v2.md` §7 contains the phased roadmap (Phases 0–5, each with a "next-build lock"), and `docs/holmes-project-orientation.md` §4 records a current Phase 0 kickoff prompt (`holmes-claude-code-kickoff-phase0-v2.md`) — **not yet in this repo**; see F-009. The pre-audit draft in `docs/roadmap/build-phases.md` is superseded by spec §7.

**F-004** | MAJOR | consistency | `docs/case-file/02-triad-context.md` §"Alfred" | Holmes deck: Holmes is "built on the open-source Goose framework"; Alfred deck: Goose is "The Hands" / the triad's execution engine; GooseClaw deck: Goose is "The Hands" with GooseClaw as "Body & Judgment" and no Holmes at all | The decks disagree about what Goose is and who runs on it — this is the substrate decision, and it's currently contradictory | Audit Phase 2 contradiction hunt; human decision D-03 on the runtime architecture | H
> **Status update 2026-07-13 (later session): RESOLVED BY SPEC.** `docs/holmes-spec-v2.md` §5: Holmes and Alfred are "true siblings on one runtime" — both on goose (`aaif-goose/goose`), driven over Zed's ACP, tooled over MCP. The NotebookLM decks are conceptual/marketing-grade and the spec supersedes them. D-03 recommended for human close-out.

**F-005** | MINOR | claims | `docs/case-file/01-blueprint-kb.md` | Blueprint's external claims are unverified: Firecracker "<1ms startup times" (attributed to the 2019 AWS paper), OpenCorporates "230M+ entities", catalog "1,300+ license-checked tools" | If load-bearing numbers are stale or misquoted, the design inherits the error | Audit Phase 1 verifies each against primary sources with dates | H

**F-006** | MINOR | provenance | earlier working notes (not in this repo) | "ACP" was attached to Holmes in working notes but appears in no source document (checked all four decks 2026-07-13) | Unsourced protocol assumptions leak into architecture as if decided | Logged as UNKNOWN in the KB; audit Phase 2 research item: what protocol connects the triad? | H
> **Status update 2026-07-13 (later session): RESOLVED.** ACP is fully sourced in `docs/holmes-spec-v2.md` §4.1: **Zed's Agent Client Protocol** (JSON-RPC 2.0 over stdio; agentclientprotocol.com), invoked via the `goose acp` command — explicitly distinct from IBM/BeeAI's "Agent Communication Protocol." The decks simply never mentioned it; the working notes were right, and the spec is the source.

**F-007** | MINOR | KB completeness | KB-wide | ABSENT: threat model, test plan, deploy runbook, data model, cost model — the standard expected-but-absent set | These are exactly the documents audit Phase 0 will demand; listing them now saves a round trip | Produce during audit Phases 0–3 | H
> **Status update 2026-07-13 (later session): PARTIALLY RESOLVED.** Spec v2 §6 covers the dual-use safety layer, supply-chain hygiene (Syft/OSV-Scanner/Grype, no Trivy, SHA-pinned Actions), and provenance/chain-of-custody; §8 covers open risks with cost/pricing data. Still ABSENT: a formal test plan, deploy runbook, and data-model document (spec Phase 0/2 locks imply them but don't specify them).

**F-008** | NIT | sources | `docs/case-file/00-provenance.md` §"Caveats" | Blueprint PDFs are OCR-noisy NotebookLM decks; diagram content is not fully captured in the text layer | Load-bearing detail could live only in a diagram | Human pass over the slides to confirm; note anything found as KB addenda | M

## Findings added 2026-07-13 (later session — canonical docs landed)

**F-009** | MAJOR | process | `docs/holmes-project-orientation.md` §4 | Artifact index lists `holmes-claude-code-kickoff-phase0-v2.md` as **Current** ("Phase 0 kickoff prompt for Claude Code Desktop, re-pointed at the spec") | The kickoff prompt is the instrument that starts Phase 0, and it is not in this repo or Drive — if it exists only on the local machine, the build can't start from a clean clone | Human supplies the file; commit it beside the spec | H

**F-010** | MAJOR | process | whole repo vs. `docs/holmes-spec-v2.md` header | Spec sync rule: "Canonical home: the Holmes Claude Code repo at `docs/holmes-spec-v2.md` — the repo copy is the single source of truth"; yet until 2026-07-13 the repo contained only a LICENSE, while the current spec, orientation doc, and kickoff prompt lived only on a local machine | The project's own drift-prevention rule was violated from day one — the declared source of truth was empty while the real artifacts were unsynced and unversioned | Resolved for spec + orientation + research doc (landed this session); F-009 covers the still-missing kickoff prompt; adopt commit-after-revision as standing practice | H

**F-011** | MAJOR | sync | claude.ai project knowledge (see `docs/case-file/00-provenance.md` §"The claude.ai Holmes project") | Project knowledge contains at least six canon docs absent from this repo, including `holmes-vs-wcjbt.md` (named a source-of-truth doc by the project instructions) and `holmes-spec-v2.1-diff.md` (implying the committed spec v2 is already superseded) | The sync rule says drafts flow back to the repo and the repo copy wins — but the repo can't win with documents it doesn't have; the committed spec may be one revision stale | Human exports the six files (or the master prompt carries their content); commit beside the spec; if v2.1 is real, land it and log the delta | H
> **Status update 2026-07-13 (later session): LARGELY RESOLVED; scope corrected.** The claude.ai project enumerated its knowledge: six files, of which all are now in the repo except the wisdom-map upload (pending; may be a v3 superseding the committed v2). `holmes-vs-wcjbt.md`, `triad-canon.md`, and `epistemic-canon-Holmes.md` landed 2026-07-13; the re-supplied spec and orientation diffed byte-identical (no drift). The screenshot's other four files were **chat attachments, not project knowledge** — reclassified: kickoff prompt stays F-009; `holmes-denylist-acceptance-criteria.md`, `holmes-spec-v2.1-diff.md` (the v2.1 threat is unconfirmed until sighted — it may be a draft diff, not adopted canon), and `Iterative quality validation process.md` must come from where they were drafted (per claude.ai: chat retrieval yields excerpts, not verbatim files — do not reconstruct from retrieval). Also newly referenced, never sighted: `claude-code-epistemic-integration-prompt.md`.
> **Final status 2026-07-13: project-knowledge scope CLOSED.** All six project-knowledge files reconciled — three committed as new canon, three (spec v2, orientation, wisdom map v2.0) diffed byte-identical to repo copies. Remaining open items are chat-attachment artifacts only (kickoff v2 → F-009; denylist acceptance criteria; spec v2.1 diff; iterative QV process) plus two dangling references: the wisdom map **v3** cited by the epistemic canon (project knowledge holds v2.0 — v3 never sighted) and `claude-code-epistemic-integration-prompt.md`.
> **Update 2026-07-13 (same session, later):** the v3 dangling reference is RESOLVED — **v3.1** supplied and committed (`docs/research/`, docx + md). v3.0's files were lost; v3.1 was rebuilt on the repo's committed v2 base with all upgrades re-verified (its Appendix B). Remaining unlocated: `claude-code-epistemic-integration-prompt.md`, plus the four chat-attachment artifacts. One cross-repo follow-up: the shared epistemic canon's "v3" citation should be updated to "v3.1" centrally (WCJBT repo) and re-propagated — not forked in the Holmes copy.
> **Update 2026-07-18:** three of the four chat-attachment artifacts have now landed on `main`: `holmes-denylist-acceptance-criteria.md` (2026-07-17 upload, hash-verified, normalized to `docs/acceptance/`), `holmes-spec-v2.1-diff.md` (applied — see F-014), and the 2026-07-13 second-pass audit report (`docs/audit/holmes-master-build-loop-v1-second-pass-audit.md` — see D-06). Still unlocated: kickoff v2 (F-009), `Iterative quality validation process.md`, `claude-code-epistemic-integration-prompt.md`.

**F-012** | MAJOR | sync | claude.ai project GitHub connector (screenshot 2026-07-13) | Connector card shows `MartinMontero/holmes` @ `main` — and `main` contains only LICENSE; the entire case file + spec live on `claude/focused-ramanujan-et67m7` | The pressure-testing surface's "repo copy wins" pointer resolves to an empty repo; every stress-test it runs is blind to the actual canon | Merge the working branch to `main` (human go-ahead required — I won't push to `main` without it) or repoint the connector at the branch | H
> **Status update 2026-07-13 (later session): RESOLVED.** Human approved the merge; working branch merged to `main` (reconciled with the pre-existing PR #1 merge commit) and pushed. `main` now carries the full canon, and the standing practice is to keep it synced after each canon commit.
> **Evidence correction 2026-07-13 (master-loop pre-flight, per its §3.0c):** the original evidence line "`main` contains only LICENSE" was true of this session's stale clone view but not of the remote at finding time — PR #1 (merge `7617fe3`) had already landed the first case-file commit on `main`. The accurate finding: **stale connector sync + stale session clone**, not empty `main`. Fetch evidence: `git log origin/main` at pre-flight shows 9 commits, a superset of the observed 3-commit state; no history rewrite. Standing practice updated by the master build loop v2: `main` lands **by PR on explicit go-ahead only**; direct merges discontinued.

## Session findings (post-case-file)

> **F-013 correction 2026-07-17 (baseline-provenance pass): reclassified MAJOR → MINOR, category sync → baseline provenance (verification-process).** The diff baseline supplied in-band 2026-07-17 was the **v1 draft, mis-attributed upstream on the pressure-testing surface** as "the drafted v2 text" (per Martin, this pass — human-attested). The recovered v2 original (157 lines, sha256 `42ab6208548534993d0322a5244cf7013c17943e4faba9ae2e4344b098ccd457`) is **byte-identical** to committed blob `dc004446` — VERIFIED this session: `git cat-file -p dc004446… | sha256sum` reproduces the hash exactly; `diff` exit 0; zero delta, not even whitespace. **No landing loss occurred**; committed L60 matches the authentic v2 original. True finding: the fidelity check diffed against an in-band baseline whose provenance was unverified — the entry's own ASSUMED label was the operative caveat. A-01's restore premise is void (see amendments). Original entry and evidence chain preserved below, invalidated not deleted.

**F-013** | MAJOR | sync | `docs/prompts/holmes-master-build-loop-v2.md` L60 (blob `dc004446` at `main` `fdda04d`) | Committed Task 3.3 omits clauses present in the drafted v2 text (draft as supplied by the human 2026-07-17 — ASSUMED, no on-disk source to verify against): the explicit tier contrast "`deepseek-reasoner` → Flash, not Pro", the priority clause "Fix or flag before anything else ships.", and "config" in "budget/config logic"; "the promo rate" altered to "the discounted rate". Dropped clauses ABSENT elsewhere in the committed file (grep `not Pro|Fix or flag|before anything else|promo|config logic`: no hits beyond Tier-1/Tier-2 noise). The committed line adds a provenance annotation not in the draft ("verified live 2026-07-13 against the DeepSeek pricing page") | Lossy landing of the QA-settled driving prompt: an executor of the committed loop lacks the explicit reasoner-tier-downgrade warning and the ship-priority constraint the draft carried; also explains part of the 2026-07-17 runbook-vs-loop ordering mismatch (the runbook tracked the draft, not what landed) | A-01 restores the dropped clauses (PROPOSED; human go-ahead required — the loop file is untouched by this finding) | H on the textual divergence; draft authenticity ASSUMED

**F-014** | MINOR | process | Task 3.2 (`docs/prompts/holmes-master-build-loop-v2.md` §3.2) vs. disk | Spec header still reads "v2 (QA-Corrected)" (`docs/holmes-spec-v2.md` L1, read 2026-07-18); `holmes-spec-v2.1-diff.md` ABSENT — searched the `holmes`, `Alfred`, and `wecanjustbuildthings.dev` working trees 2026-07-18, no match (chat-attachment artifact, per F-011 final status) | Task 3.2 cannot apply E1–E14 without the diff file, and improvising them would author canon here (prohibited) | Human supplies `holmes-spec-v2.1-diff.md` from where it was drafted; Task 3.2 then applies it exactly and re-seeds the derived files. Task skipped with approval 2026-07-18 (Martin, in-band: "3.2: stop-report, ledger as open, skip, continue 3.3 → 3.4 — APPROVED") | H
> **Status update 2026-07-18 (same day, later): RESOLVED.** `docs/holmes-spec-v2.1-diff.md` supplied by the human and landed on `main` (commit `957e369` range). Task 3.2 executed: **E1–E14 applied exactly** (each FIND verified to match once before editing); spec title now v2.1; the three remaining "Qwen3-Max-Thinking" strings are supersession language only (L36/L114/L121); marker counts post-apply: 10× `[NEEDS-CAVEAT]`, 11× `[DIRECTIONAL]`. Derived knowledge files re-seeded per the diff's Post-apply §2 (`CLAUDE.md` marked block + `docs/architecture.md`, `docs/constitution.md`, `docs/build-roadmap.md`, `docs/security.md`), markers preserved verbatim.

**F-015** | MINOR | sync | `docs/acceptance/holmes-denylist-acceptance-criteria.md` header vs. the Phase 0 build instruction (2026-07-18, in-band) | Header on disk reads "Provider-Denylist Acceptance Criteria (AC-DL) · **v2 (re-derivation)**"; the Phase 0 kickoff instruction cites the AC doc as "(**v3**)". No v3 exists in the repo (`git log --follow`: 2 commits — upload + normalize) | The build executed against the repo copy per the sync rule ("the repo copy wins"), but if a v3 was drafted upstream, the criteria the locks were verified against may be one revision stale | Human either supplies v3 for reconciliation or confirms the v2 re-derivation is current; if v3 lands, re-run the AC-DL joint gate against any changed criteria | H
> **Status update 2026-07-18: RESOLVED (dual-copy cleanup included).** The human landed **v3 (merged)** at repo **root** on `main` (sha256 `7124772b…4a1ae4bd`, gate-verified before any write), while `docs/acceptance/` still held the v2 re-derivation (`763775…9a41`) — two divergent copies. Normalized on this branch per the loop's rule: `git mv -f` root v3 → `docs/acceptance/holmes-denylist-acceptance-criteria.md` (hash preserved across the move), **root copy removed**, v2 superseded to git history. v3-conformance pass executed: implementation + tests diffed against v3 and every delta closed with locking tests — **F-021** (named-endpoint guard denial), **F-022** (documented seed list), **F-023** (multi-lockfile walk + dependency-path), **F-024** (control-naming to v3 convention + denylist≠CVE note + honest limits). AC-DL joint gate re-run green (40 tests, both controls firing).

**F-016** | MINOR | supply-chain | crates.io package `goose-cli` (API metadata fetched live 2026-07-18) | `{"name":"goose-cli","max_version":"0.2.0","description":"Step-by-step install helper. Tells you what to type, doesn't run it.","repository":"https://github.com/Montana/goose","downloads":144,"num_versions":1,"created_at":"2026-05-21"}` — not the AAIF project | Name-collision/squatter hazard on the obvious install path: `cargo install goose-cli` yields a non-AAIF artifact. Anyone (including Alfred docs or future CI) following "install goose from crates.io" gets the wrong binary | Pin every goose install instruction to source builds of `aaif-goose/goose` (or its official release artifacts) with commit provenance recorded; never `cargo install goose-cli` from the registry | H

## Phase 0 build — adversarial self-verification (2026-07-18)

> Source: an in-session adversarial pass — 7 independent skeptics, each tasked to *break* one guard property, every reported defect re-reproduced against source by a second agent before acceptance. 3 claims survived (L2 env-strip, BYOK invariant, born-redacted output). 4 confirmed defects below were **fixed and locked with regression tests in the same session** (all green, release mode).

**F-017** | MAJOR | denylist-enforcement | `crates/holmes-guard/src/proxy.rs` (absolute-form HTTP + `ProxyConfig.upstream`) | Reproduced: in the plain-HTTP absolute-form branch with an upstream forward proxy configured, only the first request line was host-validated, then `write_all(&buf)` forwarded pipelined bytes and `splice()` transparently carried keep-alive continuation to the re-parsing upstream, which re-dispatches `GET http://api.openai.com:443/...` unvalidated — no 403, no EgressEvent. Breaches deny-by-default + the vendor gate. CONNECT immune (opaque tunnel, host-pinned); direct no-upstream leg immune (origin serves its own socket, does not forward onward) | A forward-proxied Holmes could reach an excluded vendor via a smuggled second request | **FIXED**: upstream absolute-form path now fails closed on any bytes past the request head (`head_end < buf.len()` → deny) and copies the response direction only — never splices client→upstream into the re-parsing proxy. Regression: `s4_pipelined_request_through_upstream_forward_proxy_is_blocked` + `s5_single_request_through_upstream_forward_proxy_still_works` | H

**F-018** | MAJOR | denylist-enforcement | `crates/holmes-guard/src/scan/mod.rs` `check_package_name` | Reproduced: the AC-DL-2 §4 router/gateway check used exact whole-name equality (`ROUTER_GATEWAY_PACKAGES.contains(&normalized)`), so real-world variant/transitive spellings — `litellm-proxy`, `litellm-proxy-extras` (an actual litellm transitive dep), `openrouter-py`, `litellm_router` — tokenized past it and passed the gate, while the namespace branch two lines below already tokenized. A router package whose entire function is reaching excluded providers evaded a named acceptance criterion | Router-reach is constitution-equivalent to direct-reach; the gate under-enforced §4 | **FIXED**: router branch now token-scoped like the namespace branch (any token ∈ seed list fires); `litellmish` still passes (no new false positives). Regression: `c4_router_seed_matches_real_world_variant_names` | H

**F-019** | MINOR | denylist-enforcement | `crates/holmes-guard/src/policy.rs` `excluded_model_token` / `excluded_namespace_token` | Reproduced: `llama`/`grok` matched with `starts_with` (token-start only) while `gpt` used `contains`; `tokenize` splits only on non-alphanumeric boundaries, so a concatenated id like `qwenllama`/`mistralgrok`/`gemmagrok`/`mistralllama70b` is one token that starts with a permitted family prefix and embeds the excluded family mid-token → resolved `Ok`. Narrow reachability (only where the tag is fully user-controlled, e.g. local Ollama; cloud endpoints won't serve a mislabeled id), but breaches the vendor gate in principle | Deny-by-default backstop had a mid-token hole | **FIXED**: both predicates match `llama`/`grok` anywhere in the token with an exact `ollama` carve-out (`"ollama".contains("llama")` is true; `"groq".contains("grok")` is false — safe). Regressions: `excluded_family_matches_mid_token_not_just_at_start` (unit) + `s2_concatenated_excluded_family_after_permitted_prefix_denies` | H

**F-020** | MINOR | test-fidelity | `crates/holmes-guard/tests/acdl1_structural.rs` | Reproduced: the §1 structural test's `FORBIDDEN_EXTENSIONS` omitted `mts`/`cts`; it skipped symlinks (a symlinked `policy.js` would be walked past) and `node_modules`; and its docstring overclaimed — a no-TS/JS assertion is a *necessary* condition of §1 ownership, not proof of it (policy authored in Python/shell/`build.rs`/WASM is invisible to it) | An over-claimed test reads as stronger coverage than it delivers | **FIXED**: added `mts`/`cts`; symlinks with a forbidden name are flagged not followed; `node_modules` no longer skipped; docstring/assert rescoped to the honest necessary-condition claim | H

## Phase 0 build — v3-conformance deltas (2026-07-18, closed same session)

> Opened and resolved while reconciling the implementation (built to the v2 re-derivation) against the landed **v3 (merged)** AC doc. Each is a delta between what v2 specified and what v3 requires; all fixed with locking tests in the same session.

**F-021** | MINOR | denylist-conformance | `crates/holmes-guard/tests/acdl1_proxy.rs` vs. v3 AC-DL-1 §2(a) | v3 §2(a) names a *representative excluded set* — "at minimum `api.openai.com`, a Meta Llama API host, and `api.x.ai`" — each asserted blocked at the egress layer. The v2-era test covered openai/x.ai/facebook but not a Meta **Llama API** host | The named-endpoint assertion v3 requires was not explicit | **FIXED**: `s2a_named_excluded_endpoints_denied_at_guard_level` asserts guard-level 403 for `api.openai.com`, `api.llama.com`, `api.x.ai` | H

**F-022** | MINOR | denylist-conformance | `crates/holmes-guard/src/scan/mod.rs` vs. v3 AC-DL-2 §2 | v3 §2 requires a **checked-in, version-controlled list with each entry's rationale documented in-file** (seeds: openai, tiktoken, llama-stack, llama-*, xai*/grok*, litellm). v2 impl matched via bare token predicates with no documented seed table | The seed denylist was not a documented artifact per §2 | **FIXED**: `PACKAGE_SEEDS` table with per-seed match mode, class, and rationale; `check_package_name` iterates it; `c2_seed_list_covers_every_v3_documented_entry` asserts coverage. `openrouter` carried as A-06 | H

**F-023** | MAJOR | denylist-conformance | `crates/holmes-guard/src/scan/mod.rs` vs. v3 AC-DL-2 §1/§3 | v3 §1 requires **all lockfiles across all ecosystems** walked (Rust/Node/Python), the resolved transitive closure as the unit; §3 requires failure output to name **the dependency path that pulled in** a transitive offender. v2 impl walked only the single passed lockfile and reported location without a dependency path | Under-covered §1 (Node/Python) and missing §3 provenance | **FIXED**: lockfile discovery across `LOCKFILE_NAMES`; per-ecosystem parsers (Cargo/uv/poetry TOML with edges, npm, pnpm, yarn, pip); `trace_dependency_path` emits root→…→offender for edge-bearing formats. Tests: `c1_walks_lockfiles_across_ecosystems`, `c1_discovery_finds_the_real_cargo_lock_only`, `c3_transitive_violation_names_the_dependency_path` | H

**F-024** | MINOR | denylist-conformance | controls, CI workflow, `docs/security.md` vs. v3 §4/§5/§6 + "does NOT claim" | v3 fixes the control convention (**positive = gate fires on bad input**; negative = permitted-adjacent doesn't trip) — the v2 impl had them reversed; v3 §6 adds a **denylist ≠ CVE-scan** note; v3's closing section requires honest limits (neutral-name proxying, namespace completeness `[NEEDS-CAVEAT]`, weight-provenance→2e) in `docs/security.md` | Naming inverted vs v3; §6 note and honest limits absent | **FIXED**: controls + CLI + workflow relabelled to v3 convention (`c4_positive_control_*`, `c5_negative_control_*`); explicit permitted-adjacent negative control added; `docs/security.md` carries the §6 note and all three honest limits | H

**F-025** | MINOR | denylist-conformance / process | `.github/workflows/supply-chain.yml:4` @ commit `107f0a5` (caught by acdl-gate run #10, 2026-07-19) | CI failure output: `the permitted stack must pass; violations: [Violation { kind: ExcludedModelFamilyIdentifier, subject: "openai", location: ".github/workflows/supply-chain.yml:4" ... }, Violation { ... subject: "xai" ... }]` — the new workflow's header comment named the excluded vendors while describing the denylist, and AC-DL-2 scans `.github/` as config | First live confirmation the gate fires on real config drift — and it establishes the precedent for *policy mentions* in scanned config: prose that merely references the denylist can trip the same scanner that guards against real violations | **Precedent (adopted): reword-over-exemption.** Prose references to the policy inside scanned config are reworded to avoid excluded-vendor tokens (fix commit `6595499`, scanner CLEAN, acdl-gate green same day); the exemption table stays reserved for the compiled policy and its planted test fixtures — never for comments. Vendor names remain legitimate in `docs/` and markdown (unscanned by design) | H
> **Status: RESOLVED 2026-07-19 (same day).** Fix landed via PR #7 (`main` @ `b6b19dc`); both gates green on the fix commit. The precedent is recorded here so the next policy-describing comment in `.github/`/`crates/` gets reworded rather than exempted.

**F-026** | MINOR | test-fidelity | `crates/holmes-smoke/src/main.rs` verdict computation (caught live 2026-07-19, first Tier-1 endpoint run) | Harness printed `ROUND-TRIP COMPLETE; model=claude-sonnet-5; response_bytes=246` while `response_text` was goose *relaying a provider 400* ("Your credit balance is too low to access the Anthropic API") as agent text — any non-empty reply counted as a completed model leg | An executor reading the harness verdict would book a model completion that never happened — the exact overclaim class the zero-fabrication gate exists to stop (the F-020 lesson, recurring at the harness layer) | **FIXED same session**: goose's observed error-relay prefix ("Ran into this error:", goose 1.43.0 @ `8e78960e`) now yields verdict `PROVIDER ERROR RELAYED`, exit 6, documented in the exit-code table; re-run against the same billing error confirms exit 6. Transcript: `docs/audit/evidence/0c-transcript-2026-07-19.json` | H

## Phase 2 build — pre-build verification (2026-07-19)

**F-027** | BLOCKER | vendor-gate / supply-chain | `graphiti-core` 0.29.2 `requires_dist` (PyPI JSON, fetched live 2026-07-19) | Base (non-optional) dependencies quoted verbatim: `neo4j>=5.26.0`, `numpy>=1.0.0`, **`openai>=1.91.0`**, **`posthog>=3.0.0`**, `pydantic>=2.11.5`, `python-dotenv>=1.0.1`, `tenacity>=9.0.0`. The permitted LLM clients (`anthropic`, `google-genai`, `groq`) appear only as **extras**, never base | Adopting Graphiti as-is lands `openai` (an excluded-vendor SDK) directly in the Python dependency tree — a constitution #5 / AC-DL-2 violation and, per the standing gates, a **Blocker** (the AC-DL-2 gate's own seed list flags `openai`/`tiktoken`; this is the dependency-*presence* gate, orthogonal to the runtime-traffic pin the spec's "pin permitted clients" addresses at AC-DL-1 §6). Separately, `posthog` is a phone-home telemetry SDK, contrary to constitution #9 (born-redacted, local-only, no phone-home). The spec (§3.1/§4.4) named this exact risk — "Graphiti's default LLM client is OpenAI — pin permitted clients" — but resolved only the runtime leg, not the dependency-tree leg | Do **not** `pip install graphiti-core` into the committed tree. Resolution is a human decision — **D-12** (fork-and-strip / reimplement-subset / upstream-PR-and-wait). Phase 2 lock 2a build start is blocked on D-12. Confidence: **H** (primary PyPI metadata) |

## Phase 2 build (2026-07-19)

**F-028** | MINOR | denylist-conformance / process | new `crates/holmes-wall` source + `.github/workflows/acdl-gate.yml` (caught by AC-DL-2 self-scan during the Phase 2 sweep, 2026-07-19) | The AC-DL-2 scanner flagged 29 excluded-vendor identifier hits across the new Wall crate — legitimately, since `crates/` is scanned as code: prose comments naming the excluded vendor, the memory-layer's operational excluded-endpoint check, the lock-2b denial test's `CONNECT` target, and my own reworded §6 CI marker text | Second occurrence of the F-025 class, now in *enforcement code* rather than a comment: code that must name an excluded vendor to *test its exclusion* trips the same gate that guards against real violations | **RESOLVED same session, extending the F-025 precedent.** Prose reworded to name no vendor; **operational literals sourced from the single exempted home** — added `policy::EXCLUDED_CANARY_HOST`/`_PORT` (the compiled-denylist module, already scanner-exempt) and drove the memory-layer check, the 2b test, and the excluded-provider test-list (`policy::EXCLUDED_PROVIDERS`) from it. Result: **zero excluded literals in any non-exempt file and zero new exemptions** — reword-over-exemption taken to its logical end. AC-DL-2 real tree CLEAN; `c5_negative_control` green. Confidence: H |

## Findings relayed from the pressure-testing surface (2026-07-19, via Martin)

**F-029** | MINOR | corroboration-heuristic evasion / lock-1a | `crates/holmes-core/src/analysis/emission.rs` `source_root` (upstream source review, relayed by Martin 2026-07-19; re-reproduced against source this session) | The URL branch took everything between `://` and the first `/?#` as the host — **userinfo included** (`https://user@example.org/x` → `user@example.org`, not `example.org`), so decorated duplicates of **one** source satisfied the ≥2-independent floor; the **trailing-dot FQDN** form (`example.org.`) also counted as distinct from `example.org`; and **subdomains of one registrable domain** count as distinct roots | Deterministic, free evasions of the lock-1a corroboration gate — provenance forgery by URL decoration | Strip userinfo (mirroring the L1a proxy's s4 rule, which rejects it) and normalize trailing dots; collapse-to-registrable-domain needs a public-suffix list, so subdomain granularity is instead **documented explicitly** in the heuristic note; add provenance-forgery shapes (decorated URLs, subdomain pairs) to the **Phase 2.5 adversarial corpus** | H
> **RESOLVED (userinfo + trailing-dot) same session:** `source_root` drops userinfo and trailing dots; regression `userinfo_cannot_fabricate_independent_roots` locks both plus the documented subdomain granularity. **CARRIED:** subdomain collapse (public-suffix decision) and the forgery shapes → Phase 2.5 adversarial corpus.

**F-030** | NIT | evidence bookkeeping | `STATE.md` Phase 1 inventory (1b row) vs PR #10 body vs `docs/audit/evidence/1b-case-transcript-2026-07-19.json` | STATE.md records "54 frames"; PR #10's body says 64; the committed transcript's `frame_count` is **64** (verified this session) | The 54 was the *first* 1b run's count; the committed evidence is the re-run on the F-026-fixed harness, and STATE.md was not refreshed — a doc/evidence mismatch, not an evidence problem. PR #10's body was already correct | Reconcile to the transcript: STATE.md → 64 with a superseded-figure note | H
> **RESOLVED same session:** STATE.md corrected to 64.

## F-029 fix — adversarial re-verification (2026-07-20)

> Method: while confirming the relayed F-029/F-030 resolutions against the tree, an adversarial pass attacked the committed `source_root` fix — 3 independent lenses (URL-spec normalization, caller reachability, non-URL branch), every claimed shape re-traced against source (and reachability confirmed through `Provenance::new`, which rejects only empty strings) before acceptance; 4 claims rejected as invalid-input-only or already-documented granularity. The F-029 resolutions themselves held (regression green; PR #10 body already said 64; transcript `frame_count` 64 with a 64-element `frames` array). The surviving shapes are one finding:

**F-031** | MINOR | corroboration-heuristic evasion / lock-1a | `crates/holmes-core/src/analysis/emission.rs` `source_root` + `emit` | Confirmed by code-trace, each shape yielding ≥2 distinct roots for ONE source: **port decoration** — `https://example.org/a` vs `https://example.org:443/a` → `example.org` vs `example.org:443` (likewise `:80`, non-default ports, and the empty-port form `host:`); **scheme-less citation** — `example.org/page-1` vs `example.org/page-2` fell to the path branch and minted one root *per page* of one site, no decoration needed; **empty roots counted** — `#appendix` and `file:///…` normalized to `""`/authority-less forms yet `""` satisfied the floor as a distinct root; **whitespace/backslash/slash-count spellings** — interior TAB in the host, `https:\\example.org\a`, `https:///example.org/a`; **double trailing dot** — `example.org..` defeated F-029's single `strip_suffix('.')`; **file-path spellings** — `./`, `//`, `/./`, `/../`, `\`-separated, attached `§`, trailing `,`, and `:line` each minted a new root for one file | Same forgery class F-029 opened: deterministic, free decorations of one source clear the ≥2-independent floor at emission | **FIXED same session:** tab/CR/LF stripped and `\`→`/` folded before parsing; authority-less URLs fall through to the path rules; `:port` shed (digits-or-empty suffix, IPv6-bracket-safe) with trailing dots trimmed both sides of the port strip; the path branch normalizes lexically (`//`, `.`, `..`), cuts `§`/`,`/`:line` decorations, and collapses a host-like first segment to the host rules; the gate counts only **non-empty** roots and its denial message says so. Regressions: `port_path_and_scheme_decorations_cannot_fabricate_independent_roots`, `empty_roots_do_not_count_toward_corroboration` (104 workspace tests green, release `--locked`; AC-DL-2 + recipe scans CLEAN). **CARRIED → Phase 2.5 adversarial corpus** (joining F-029's carried shapes): IPv4 numeric forms (`3232235521`, `0xC0.0xA8.0.1`, `127.1`), IPv6 compression (`[::1]` vs `[0:0:0:0:0:0:0:1]`), IDN Unicode-vs-punycode + non-ASCII case folding, percent-encoded hosts, single-slash scheme (`https:/example.org`), absolute-vs-relative and `file://`-vs-relative spellings of one file, parent-relative (`../`) prefixes — each needs canonicalization machinery (IP/IDN/percent parsing) or repo-root knowledge this floor heuristic deliberately does not take; subdomain granularity stands as documented | H

## Housekeeping session (2026-07-20)

**F-032** | MINOR | doc-sync | `README.md` L17–L26 @ `cf45f6e` | Front page stated "Project status: SPEC'D, PRE-PHASE-0 — no code yet" and "This repository contains **no code yet**" while `main` at the same commit carries a four-crate Rust workspace, Phases 0–2 closed via PRs #7–#13, 104 workspace tests green, and executed evidence in `docs/audit/evidence/`; the case-file map and Next Steps likewise predated the build (map showed docs only; Next Steps still pointed at "Phase 0 build begins") | The repo's front page contradicted the tree it fronts — the most-read document failed the evidence-or-it-didn't-happen gate on the project's own state | **FIXED this session:** Project Status rewritten to Phases 0–2 CLOSED with executed evidence, Phase 2.5 next as a hard gate, language bar Rust; case-file map and Next Steps updated to the current tree; triad table, invariant quote, and Standing Gates preserved unchanged | H

**F-033** | NIT | repo-hygiene | remote `claude/*` branches @ origin, verified post-`fetch --prune` 2026-07-20 | All five remote `claude/*` branches verified fully merged — `git log origin/main..origin/<branch>` → **0 commits** for each of `focused-ramanujan-et67m7`, `holmes-launch-runbook-kwtkta`, `ledger-upstream-source-review-litx08`, `phase-0-holmes-guard-build-va1er0`, and the designated `claude-code-git-bash-path-f30buf` (retained — it carries this session's PR) | Stale fully-merged remotes accumulate and invite branch-picking mistakes | Deletion of the four non-designated branches **attempted and BLOCKED in this environment** — `git push origin --delete` → HTTP 403 at the push layer (batch and single-ref both; same denial class as prior classifier/proxy blocks, not routed around per the standing rule), and the GitHub MCP roster has no branch-deletion tool. **Carried to Martin:** delete the four via the GitHub UI (each shows 0 commits ahead; merged-PR pages offer one-click Delete branch) | H

## Phase 2.5 build — adversarial verification (2026-07-20)

> Method (loop §6 Phase 2.5(v), MANDATORY): after the four locks compiled green, an independent skeptic was fanned at **each** of 14 defense claims — one attacker per claim, told to REFUTE by finding a concrete code-traced bypass, then a second independent skeptic re-checked every non-HOLDS verdict against source. 17 agents, 0 errors. Result: **12 HOLDS, 3 breaches** (each corroborated PLAUSIBLE_BREACH — a real defense-mechanism gap, though staging a committed leak for name-firewall needs an out-of-repo caller). All three reproduced against source and fixed with locking regressions **before** this checkpoint. The attacks that SUCCEEDED, and their fixes:

**F-034** | MAJOR | injection-defense / lock-2.5a | `crates/holmes-core/src/safety/reader.rs` `expose_raw_to_quarantined_backend` + `tests/safety_locks.rs` name-firewall test | The raw-bytes accessor was `pub fn` on the publicly re-exported `UntrustedContent` (`safety/mod.rs` → `pub mod safety`), so a **cross-crate** privileged caller — a workspace sibling or Alfred, the actual embedder — could call `content.expose_raw_to_quarantined_backend()` and read hostile bytes; the lock-2.5a firewall test only scanned `holmes-core/src`, so it could not see that reach. The narrow claim held (it is the only accessor on the type; no Deref/Display/Clone/serde/AsRef leak), but the "privileged module cannot reach raw bytes even deliberately" claim was false as tested | The whole 2.5a guarantee is "the privileged planner never sees raw hostile bytes," and the planner is cross-crate; a test whose scope is narrower than the accessor's reachability is a mechanism gap, not proof | **FIXED same session:** accessor downgraded to **`pub(crate)`** (compiler forbids any external crate from calling it — the reach is closed structurally, not by grep), and the firewall test broadened to scan **every `crates/*/src` tree** in the workspace (asserts ≥3 crate sources) so no sibling can name the identifier either. Regression green; 130 workspace tests pass | H

**F-035** | MINOR | injection-defense / lock-2.5a | `crates/holmes-guard/src/scan/recipes.rs` `smuggling_class` (via `reader.rs` validator delegation) | The reader's invisible-Unicode guard delegates to `smuggling_class` plus `char::is_control()` (Rust `is_control` = general-category Cc only). Several invisible/default-ignorable code points fell through both and were minted into an `Extraction` intact — surviving into a provenance quote: **U+034F** (combining grapheme joiner, Mn), **U+2065** and **U+206A–U+206F** (the reserved/deprecated-format gaps between the covered invisible-operator and bidi-isolate sub-ranges), **U+FFF9–U+FFFB** (interlinear annotation), and the blank fillers **U+115F/U+1160/U+3164/U+FFA0** | The reader claims to reject invisible/deceptive Unicode; accepting several classes of it is a real (if defense-in-depth) gap — the authority model ("text has no path to authority") was untouched, so MINOR | **FIXED same session** via the growable-floor process the module already documents: `smuggling_class` extended to cover U+034F, U+115F–U+1160, U+17B4–U+17B5, U+2065, U+206A–U+206F, U+3164, U+FFA0, U+FFF9–U+FFFB, U+1D173–U+1D17A (strengthens recipe-scan too — the shared vocabulary). Regression `newly_covered_invisibles_are_rejected`; recipe-scan controls both still fire (real CLEAN, planted 21 hits); no false positives on legitimate text | H

**F-036** | MAJOR | legal-guardrail / lock-2.5d | `crates/holmes-core/src/safety/subjects.rs` `Consent` | `assess_disclosure` was correct (covers every `InfoClass` identically, allow-token sealed), but its **input** `Consent::GivenInWriting { record: String }` was a public variant with a public field — unlike `SubjectScope` (which carries the "declared deterministically, never model-inferred" contract and refuses `PrivateIndividual` structurally), `Consent` trusted its own tag. Any path — including a privileged planner acting on untrusted content that *claims* "the person consented" — could mint `Consent::GivenInWriting { record: "no such record exists" }` and pass the doxxing gate | Consent-forgery / authority laundering through the disclosure gate — the exact "untrusted content becomes authority" class Phase 2.5 exists to stop, at the most sensitive gate (anti-doxxing) | **FIXED same session:** consent sealed into a `ConsentRecord` type (private witness field; only mint is `ConsentRecord::record(reference)`, which rejects an empty/absent reference — the "no such record" forgery — and is the deliberate deterministic-operator-side call, same standard as `SubjectScope`/`Knowability`). `Consent::GivenInWriting(ConsentRecord)`. Regression `consent_record_rejects_an_empty_reference`; the 2.5d disclosure lock updated to mint via the sealed constructor | H

> **12 defenses HELD under independent attack** (tried, could not break, code-traced): reader Debug born-redaction; extraction-carries-no-authority (no text→grant/phase/handoff path); reader boundary-confusion (unrequested-kind rejection); emission uncalibrated-confidence block + the core's inability to mint `Calibrated`; low-knowability bare-high-confidence block; downgrade integrity (invalidation-not-deletion, cap < floor); approval deny-by-default + unforgeable `ToolGrant`; approval log born-redaction; private-individual targeting refusal + terminal decline; person-naming threshold (3 roots / quotes / uncertainty; decorated duplicates cannot reach the floor); safety-layer handoff-only (no action API).

## Phase 4 build (Observability & hardening, 2026-07-20)

> Beta scope: **D-14 = Option A (analytical open beta)** decided by Martin 2026-07-20; Phase 4 hardens the analytical surface only (rider (a): the investigative surface is *absent* from the beta, not dark-flagged). New `crates/holmes-core/src/observability/` (`telemetry`, `permission`); telemetry hooks threaded additively through the six-phase case. Lock 4a (telemetry content-free) + the deny-by-default permission manifest + the D-14 riders green; lock 4b is the audit below.

**F-037** | NIT | honest-labeling / lock-4a | `crates/holmes-core/src/observability/telemetry.rs` module docs (surfaced by the Phase 4 security audit, 2026-07-20) | The telemetry docs claimed a caller **cannot** put runtime content into the `&'static str` class-name fields, presenting a construction-site discipline as a pure type-level guarantee. Two independent auditors flagged it: `Box::leak`/`String::leak` can mint a `'static str` from a runtime `String`, so the type alone does not forbid runtime content — the real guarantee is *closed vocabulary + no code leaks*. Both rated the code **safe as written** (no leak exists anywhere; every payload `&'static str` comes from a compiled literal via a pure `label()`/`class()` match) — the defect was the overclaiming prose, exactly the caveat-hardened-into-fact the zero-fabrication/honest-labels gate forbids | An overstated safety guarantee in a safety phase is a doc-integrity defect even when the code is sound: a reader trusts "cannot" as type-level and stops checking the construction sites | **FIXED same session:** docs reworded to state the guarantee precisely ("closed vocabulary + no-leak", with the `leak` loophole named as the honest caveat), and the no-leak half **promoted from discipline to enforced invariant** — new structural regression `observability_locks::telemetry_feeding_source_contains_no_leak` scans all of `holmes-core/src` and fails if any `.leak(`/`::leak(` constructor appears (same grep-invariant pattern as the raw-bytes name-firewall). Also reworded the "in-memory ring" comment → "in-memory buffer (unbounded Vec; growth embedder-managed)" per the same audit. 143 workspace tests green | H
> **Lock 4b — Skillsmith-style security audit PASSED (2026-07-20).** 6 independent auditors (+ adversarial corroboration on any finding) attacked the Phase 4 surface across telemetry content-freedom, opt-in/local-only, deny-by-default permission, D-14(a) investigative-absence, correlation-not-content, and instrumentation-is-additive. **Result: 6/6 HELD, zero confirmed findings, zero criticals.** Defenses that held under code-traced attack: telemetry content-free by closed-vocabulary construction (every emit site traced — `label()`/`class()` return literals, adjacent content Strings diverted to the case file/error path, never telemetry); opt-in cannot be tricked into recording while disabled and no `Deserialize`/interior-mutability forges `Enabled`; no I/O / no phone-home (only `std::sync::atomic` imported); `decide` deny-by-default is total (`None => Denied`, no third arm, private `capabilities`, no runtime widening); investigative absent by construction + two locks; `CorrelationId` a bare counter, not content or wall-clock; instrumentation side-effect-free w.r.t. control flow. The lone honesty nit (F-037) was pre-empted before the audit closed. Non-defects the auditors declined to inflate are recorded in their reasoning (e.g. `decide` not yet wired to a runtime dispatch layer — forward-looking, outside the beta surface).

## Audit findings (Phases 0–7)

*None yet — the fresh adversarial audit has not run. Note: a v1→v2 QA pass predates this ledger and its corrections are folded into `docs/holmes-spec-v2.md` ("What changed from v1") and `docs/research/wisdom-intuition-knowledge-judgment-v2.md` (Corrections Log). Append new audit findings below.*
