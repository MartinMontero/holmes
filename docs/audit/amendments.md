# Amendments — Holmes

Format: `A-## | Document | Exact change | Finding(s) resolved | Status (PROPOSED / APPROVED / LANDED)`

Ordered by dependency, then severity. Per Rule 9, every amendment is PROPOSED until the human approves it.

*Amendments are an output of the audit (Phase 7) or of ledgered session findings. Append below.*

**A-01** | `docs/prompts/holmes-master-build-loop-v2.md` L60 | Original restore premise **void** per the F-013 correction 2026-07-17 (no clauses were dropped; the diff baseline was the mis-attributed v1 draft — committed L60 matches the authentic v2 original, sha256 `42ab6208…d457`). Awaiting Martin's pick between: **(a) forward enhancement** — inside L60's parenthetical, append one clause and nothing else: `(a capability change, not a rename)` → `(a capability change, not a rename; \`deepseek-reasoner\` → Flash, not Pro)`; or **(b) WITHDRAWN**. Nothing applied. | F-013 (as corrected) | **APPROVED: pick (a) — Martin, 2026-07-18, in-band ("A-01 ruling: A"). Applied to L60 on the session branch (one clause, nothing else); LANDED on PR merge.**
> Supersedes the original A-01 three-clause restore proposal (premise void; original wording preserved in git history, commit `1b56593`).

**A-02** | `docs/holmes-spec-v2.md` §7 Phase 0 | The line specifying an "Independent **Apache-2.0-compatible** repo" is superseded by D-01 (DECIDED 2026-07-18: AGPL-3.0-or-later, matching Alfred). Spec revision to state the license per D-01 is pending — spec text is canon authored on the pressure-testing surface, so this amendment records the delta rather than editing the spec here. Carried per loop §3.2 ("license-per-D-01 … as explicit amendments pending spec revision"). | F-001 / D-01 | PROPOSED

**A-03** | `docs/holmes-spec-v2.md` §7 Phase 0 (and §4.1 shell references) | The "Tauri 2 + SolidJS shell" element of spec Phase 0 has no home in the loop's §6 phase plan: Holmes is a library-shaped component **embedded in Alfred** (settled architecture per the loop header and launch runbook §0) — the shell/surface belongs to Alfred. Flagged during Task 3.4 roadmap reconciliation (2026-07-18) per the loop's anything-dropped-becomes-an-A-## rule; spec revision pending (same batch as the standing embedded-in-Alfred amendment the loop §3.2 names). | — (Task 3.4 rule; no F-item) | PROPOSED
> 2026-07-18 (post-3.2): survives the v2.1 diff — E1–E14 do not touch §7's shell line.

**A-04** | `docs/holmes-spec-v2.md` §4.6 / §7 Phase 0 | Carry the loop §6 guard split into the spec: **L1a** deny-by-default egress proxy (core-owned), **L1b** provider/model-id resolution guard (unknown ids rejected), **L2** sanitized spawn (env stripped, proxy injected, id validated pre/post handshake), L3 deferred-recorded. Spec currently states the denylist as "runtime guard + regression test" without the layer architecture. Standing amendment per loop §3.2 ("L1+L2 guard"). | — (loop §3.2 carry) | PROPOSED

**A-05** | `docs/holmes-spec-v2.md` §7 (roadmap) | Spec-v2.2 pipeline per the second-pass audit's **A11** (report on disk 2026-07-18): add **Phase 2.5 — Safety before surface (hard gate)** and **Phase RC — Open-beta release candidate** to §7; record the **Beta Scope Decision** checkpoint; carry the **D-01 outcome** (AGPL-3.0-or-later — supersedes §7's "Apache-2.0-compatible" line, folding in A-02); resolve the §4.1 secrets `[NEEDS-CAVEAT]` when the goose docs lines are cited. | F-014 context; audit A11 | PROPOSED
> 2026-07-18 (Phase 0 build): the §4.1 secrets caveat is now primary-citable — `GOOSE_DISABLE_KEYRING` verified in source at `aaif-goose/goose` @ `8e78960e`, `crates/goose/src/config/base.rs` (keyring behind the `system-keyring` cargo feature; the pinned Holmes build compiles it out entirely). Queue the spec edit with this citation.

**A-06** | `crates/holmes-guard/src/scan/mod.rs` (`ROUTER_GATEWAY_PACKAGES`) + `src/policy.rs` (`EXCLUDED_PROVIDERS`) | Grow the AC-DL-2 §4 router/gateway seed list by ledgered amendment, per the AC doc's own growth rule: seed `litellm` [CARRIED — facts ledger] **plus `openrouter`** (function is routing to many vendors including all three excluded ones; goose ships an `openrouter` provider with `OPENROUTER_API_KEY`/`OPENROUTER_HOST` — verified in source @ `8e78960e` — so the id is also excluded at L1b resolution and its env vars stripped at L2). Reaching excluded vendors through an intermediary is still reaching them (constitution #2). | — (AC-DL-2 §4 growth rule) | PROPOSED — implemented on the session branch; lands with the Phase 0 PR on explicit go-ahead

**A-08** | `docs/holmes-spec-v2.md` §3.1 ("Memory architectures") + §4.4 ("The Wall") | Per D-12 (DECIDED 2026-07-19): **Graphiti is dropped** — `graphiti-core` base-depends on `openai` (excluded vendor, constitution #5/AC-DL-2) and `posthog` (phone-home, constitution #9); see F-027. The Wall is an **owned temporal-graph subset** on **Neo4j Community Edition** (D-09: GPLv3, run as a separate supervised service) via a denylist-clean Bolt driver, keeping Graphiti's load-bearing *properties* as requirements rather than its code: bi-temporal model (occurred-at + ingested-at), explicit validity intervals, **invalidate-not-delete**, provenance on every fact. Spec text edit pending on the pressure-testing surface per the sync rule; this entry records the delta. Kuzu-abandonment and FalkorDB notes unaffected except FalkorDB now also carries the D-09 SSPL rejection. | F-027 / D-09 / D-12 | PROPOSED — implementation lands with the Phase 2 PR on explicit go-ahead

**A-07** | `docs/holmes-vs-wcjbt.md` §6.2 (`evidence_pack` schema) | Phase 1 schema amendment per loop §6 ("record as an A-## to §6.2") and epistemic canon Upgrade B: `evidence_pack` gains **`knowability`** (high/low validity, assigned deterministically *before* the confidence score — shares vocabulary with `intuition_validity`) and structured **`limits_of_this_finding`** (what would change the conclusion / what could not be checked / where the evidence runs out) alongside the existing `confidence`/`provenance`/validity fields. Implemented 2026-07-19 in `holmes-core::artifacts` (`Knowability`, `LimitsOfThisFinding`; both enforced at the lock-1a emission gate). LR scores additionally carry a `CalibrationStatus` (Uncalibrated default; gating lands Phase 2.5). | — (loop §6 Phase 1 instruction) | **LANDED 2026-07-19** — upstream canon diff (`docs/holmes-vs-wcjbt-a07-diff.md`, authored on the pressure-testing surface per the sync rule) applied on the session branch: **E1** (evidence_pack fields) FIND-verified exactly-once and applied; **E2** (research_brief `stated_confidence`, a companion edit beyond A-07's letter) **APPROVED by Martin in-band 2026-07-19** and applied; confidence markers unchanged (3 NEEDS-CAVEAT / 7 DIRECTIONAL before and after). Post-merge propagation (byte-identical re-copy to the WCJBT/Alfred surfaces + connector re-sync + reconciling the `holmes-vs-wcjbt.md.md` duplicate) is the human's upstream step per the diff's §Propagation
> 2026-07-20 (housekeeping): the `holmes-vs-wcjbt.md.md` duplicate is **ABSENT from this repo** — verified `git ls-files | grep -i 'md\.md'` and a full-tree `find -name '*.md.md'` both return nothing at `cf45f6e`. The duplicate observed earlier lives on the project-knowledge surface (the pressure-testing project's uploaded copy), so the in-repo half of §Propagation has nothing to reconcile; the upstream re-copy + connector re-sync + duplicate removal remain the human's step
