# Build Phases — derived from the Master Build Loop v2 §6

**Status: DERIVED (2026-07-18).** Source of truth for sequencing and gates: `docs/prompts/holmes-master-build-loop-v2.md` §6 (per its precedence rule: spec wins on architecture, the loop wins on sequencing/gates, the constitution wins on everything). Architecture depth lives in `docs/holmes-spec-v2.md` §7; this file is the phase index. Every lock is met only with executed evidence.

- **Phase 0 — Scaffold, guard, ACP, embedding contract, CI.** Library layout (`crates/holmes-core/`, `crates/holmes-guard/`); guard in three layers, all policy in compiled Rust — L1a deny-by-default egress proxy, L1b provider/model-id resolution guard, L2 sanitized spawn; headless ACP round-trip harness; embedding contract (`research_brief` in; `evidence_pack`/`case_file` out); supply-chain CI. *Locks: AC-DL-1 §§1–5+§7 and AC-DL-2 (all seven) green in one CI run (§6 scheduled to lock 2b); ACP round-trip transcript on a permitted model; embedding contract + consumer test; CI green with CLAUDE.md protected block intact.*
- **Phase 1 — Analytical core.** Three engines + six-phase case state machine as goose recipes/subagents; ACH matrix; LR scorer with calibration status; devil's-advocate subagent; Orchestrator-Worker + Evaluator-Optimizer. *Locks: ACH + ≥2-independent-source corroboration gate enforced at emission; one full analytical case end-to-end; blueprint-type CI check; recipe safety scan (ASCII-smuggling strip) live from the first recipe.*
- **Phase 2 — The Wall.** Self-hosted Graphiti temporal graph (Neo4j or FalkorDB — Kuzu archived 2025-10-10); Adaptive RAG router; bi-temporal Episode provenance. *Locks: fact-to-episode traceability + invalidation-not-deletion by test; **AC-DL-1 §6 lands here**; Tier-2 ingestion quality evidenced; bundled backend supervised; AC-WP weight provenance fail-closed.*
- **Phase 2.5 — Safety before surface (HARD GATE).** CaMeL-style dual-LLM injection defense; calibration/`knowability` gating; tool-approval protocol over ACP; legal/defamation guardrails (handoff-only resolution). *Locks: hostile-content suite green; knowability gate proven; approval round-trip headless; anti-doxxing refusals tested. Nothing in Phase 3 starts before every 2.5 lock is green.*
- **→ Beta Scope Decision (BLOCKED, human's D-##):** analytical-only beta (A) vs full-surface beta (B). Not picked by agents.
- **Phase 3 — Investigative mode.** Public-records/OSINT/link-analysis MCP servers; OpenAleph; Firecracker microVM (E2B OSS self-hosted), offline-by-default for model-generated code. *Locks: every tool gated behind the 2.5 layer (bypass-attempt proven); surveillance-detection asymmetry enforced; sandbox suite incl. the `/proc/self/root` bypass pattern.*
- **Phase 4 — Observability & hardening.** Born-redacted, opt-in, local-only telemetry; deny-by-default permission manifest; trace correlation; smuggling regression stays green. *Locks: telemetry provably content-free; Skillsmith-style security audit to zero criticals.*
- **Phase 5 — Accountability layer.** NIP-32 non-destructive labeling with provenance; appeals with second-reviewer adjudication; Polis-assembly hook **stubbed**; local transparency log; CARE consent gating. *Locks: labels attach-never-delete and queryable; appeal round-trip; transparency log complete; interim-governance state documented honestly (the interim "block" is the human reviewer).*
- **Phase RC — Open-beta release candidate.** No Holmes-standalone UI/installer/updater — the surface and distribution are Alfred's. Ship-shape embedding in Alfred's CI; non-developer first run; sovereign offline path with empty egress log; full beta safety re-run; model roster RC audit (alias audit re-confirmed post-2026-07-24); honest-limits docs; compliance (license notices per D-01, SBOM, proposed-not-pushed tags). *Locks: all with executed evidence; Rule 9 on tag/release/merge.*

**Deltas vs the 2026-07-13 pointer version of this file (spec §7 summary):** Phase 2.5 and Phase RC added; smuggling scanner moved to Phase 1; weight provenance added to Phase 2; the Beta Scope Decision checkpoint inserted; the "Tauri 2 + SolidJS shell" item in the old Phase 0 line has no home in the loop's plan (Holmes is embedded in Alfred — settled architecture) and is **flagged as A-03, not silently dropped**.

Entry point: the Master Build Loop v2 itself (it superseded the kickoff-v2 entry point; `holmes-claude-code-kickoff-phase0-v2.md` remains unlocated — F-009). The pre-audit Phases A–F draft remains preserved in git history (commit `762751a`); the 2026-07-13 spec-§7 pointer version is in history before this commit.
