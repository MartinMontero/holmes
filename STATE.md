# STATE.md — Holmes build state (loop §5)

**Updated:** 2026-07-20 (Session 7 — **housekeeping**: README re-synced to build reality (F-032); A-07 §Propagation in-repo half verified nothing-to-reconcile; branch hygiene verified, deletions env-blocked → carried (F-033)) · prior: Session 6 Phase 2 (PR #11) + A-07 landing (PR #12) + F-031 hardening (PR #13, separate session); Session 5 Phase 1 (PR #10); Phase 0 CLOSED (PRs #7–#9) · **Maintainer:** the loop; refreshed at every checkpoint.

## Git state

- Current branch (Session 7): `claude/claude-code-git-bash-path-f30buf`, restarted from `main` after PR #12 merged (housekeeping). Prior phase PRs: #6 (Phase 0 guard), #7 (0e), #8/#9 (Phase 0 close), #10 (Phase 1), #11 (Phase 2), #12 (A-07 + F-029/F-030), #13 (F-031).
- `origin/main` head: `cf45f6e` (merge of PR #13 — F-031 `source_root` hardening); this session builds on it.
- Ancestry: FF-OK
- Landing mechanics: PR-only to `main`, explicit go-ahead (D-08); connector re-sync after every canon merge (F-012)

## Gate Zero (loop §3.0) — CLEARED 2026-07-18

| Call | Decision | Record |
|---|---|---|
| (a) Charter verdict | 2026-07-13 second-pass audit accepted (SHIP WITH FIXES) | D-06 · report on disk `docs/audit/` |
| (b) D-01 license | AGPL-3.0-or-later ratified; LICENSE byte-identical to Alfred's | D-01, F-001 |
| (c) Git reconciliation | Branch restart + one approved `--force-with-lease`; nothing lost | D-07 |
| (d) Discipline | Confirmed | D-08 |

## Task 0 status (loop §3) — COMPLETE

3.0 ✅ · 3.1 ✅ (landed via PR #5) · 3.2 ✅ (spec v2.1, E1–E14, derived files re-seeded; F-014 resolved) · 3.3 ✅ (alias audit; primary re-verification still egress-blocked — carry) · 3.4 ✅ (roadmap reconciled; A-03). Standing spec amendments: A-02…A-05 (+ A-05 note: `GOOSE_DISABLE_KEYRING` now primary-cited from source @ `8e78960e`, `crates/goose/src/config/base.rs`).

## Phase 0 lock inventory (loop §6)

| Lock | Status | Evidence (executed 2026-07-18, this container) |
|---|---|---|
| **0a** — AC-DL-1 §§1–5, §7 | **VERIFIED (local, release, hermetic) — v3-conformant** | `cargo test --release --locked -p holmes-guard` → **40 passed / 0 failed** (resolution §2/§5, proxy §2a/§4/§5 with planted proxy-honoring server + planted upstream forward proxy + named-endpoint denial, spawn §3, structural §1, unit; incl. regression + v3-delta tests). §6 SCHEDULED to lock 2b, recorded visibly in CI. **CI-run leg PROVEN 2026-07-19**: acdl-gate run #9 green on `main` head `3af25d9` (first real CI execution), then green on PR #7's fix commit `6595499` (both push- and PR-triggered runs) after correctly catching F-025 on run #10 — the gate demonstrably fires and passes in CI, not just locally |
| **0b** — AC-DL-2 all seven | **VERIFIED (local, release) — v3-conformant** | v3 control convention: **positive** control (planted lockfile) → FAIL / exit 1; **negative** control (real tree, lockfile discovery) → CLEAN / exit 0. Multi-ecosystem lockfile walk (§1), documented seed table with rationale (§2), dependency-path in failure output (§3 — `pulled in via holmes-app -> middleware-lib -> async-openai`). Criteria 1–7 each covered by named tests; c7 = joint workflow, same run as 0a |
| **0c** — ACP round-trip | **COMPLETE (2026-07-19, account funded)** | Funded re-run, same container/command: verdict **ROUND-TRIP COMPLETE**, exit 0 — `anthropic`/`claude-sonnet-5` L1b-permitted pre+post handshake, L2 sanitized spawn, 14 ACP frames, egress **1/1 `api.anthropic.com:443 allowed`** via L1a, streamed model completion `"pong"` (the smoke prompt's exact requested reply; 4 bytes; verdict from the F-026-fixed harness, which cannot count error relays as completions). Born-redacted transcript: `docs/audit/evidence/0c-transcript-2026-07-19-complete.json`. Prior blocked-run evidence retained below, superseded not deleted |
| *0c prior evidence (superseded 2026-07-19)* | *PARTIAL — guard/protocol legs proven, completion credits-blocked* | 2026-07-19, this container: goose rebuilt from the pin via `scripts/build-goose.sh` (`aaif-goose/goose` @ `8e78960e`, v1.43.0, trimmed features, binary sha256 `439a282e…7056`, provenance file beside the binary). `holmes-smoke --provider anthropic --model claude-sonnet-5 --credential-env MY_ANTHROPIC_API_KEY`: pre-handshake L1b permitted → L2 sanitized spawn → real `goose acp` handshake + session/new + prompt (12 frames) → post-handshake configOptions pair (`anthropic`/`claude-sonnet-5`) L1b-**permitted** → egress events **1/1 `api.anthropic.com:443 allowed`** through L1a. The endpoint **authenticated the key** (HTTP 400 billing — "credit balance is too low" — not 401 auth); goose relayed the error as agent text; harness verdict `PROVIDER ERROR RELAYED`, exit 6 (honest verdict added this session — F-026, fixed + re-run confirmed). Negative control: `openrouter` → typed pre-handshake denial, exit 3. Transcript born-redacted (verified: no key material) at `docs/audit/evidence/0c-transcript-2026-07-19.json`. **Remaining to COMPLETE: fund the API account, re-run the same command, expect exit 0 with a model completion. Never faked** |
| **0d** — embedding contract | **COMPLETE (Session 4, 2026-07-19)** | `crates/holmes-core`: §6.2 artifact types as committed canon (`ResearchBrief` in; `EvidencePack`/`CaseFile` out — types and validation only), invariant 5 enforced at construction (empty provenance / out-of-range confidence unrepresentable), invalidation-not-deletion (supersede flags `valid_until`, no removal API), handoff-only terminal state (journalist/lawyer/community/interim human reviewer; no execute/apply API), read-only provider seam re-exporting guard rosters + resolution (`holmes_core::provider`). Zero third-party deps (workspace-internal `holmes-guard` only). Evidence: 9 new tests green in release `--locked` (4 unit + 3 Alfred-shaped consumer `embedding_contract.rs` + 2 structural `no_blueprint_exports.rs` incl. F-020-style fires-check); workspace suite 49/49; clippy clean, fmt clean; CI runs the workspace suite in the acdl-gate job ("Lock 0d" step). Alfred's artifact-level guard-test obligation: already ledgered in the cross-repo table below. Phase 1 schema amendment (`knowability`/`limits_of_this_finding`) deliberately not pre-implemented |
| **0e** — full CI (SBOM/scanners) | **CVE gate PROVEN in CI (2026-07-19)** | `.github/workflows/supply-chain.yml`: Syft SBOM (SPDX+CycloneDX) + OSV-Scanner (primary, exit 1 on any vuln) + Grype (cross-check, `--fail-on high`, exit 2). No Trivy (CVE-2026-33634). Action-free ⇒ SHA-pinning satisfied by construction; scanners pinned (syft `v1.48.0` / osv-scanner `v2.4.0` / grype `v0.116.0`), checksum-verified fail-closed. **Executed CI evidence:** run #1 on `107f0a5` → success in 51s (2026-07-19 07:30:49Z — pinned installs verified against real release checksums, SBOM generated, OSV clean, Grype clean); re-run green ×2 on fix commit `6595499`; landed on `main` via PR #7 (`b6b19dc`), all four checks green at merge. Still open in 0e: provenance/attestation (spec §6.6) — carry; human: mark `acdl-gate` + `supply-chain` required checks on `main` |

## Phase 1 lock inventory (loop §6, Session 5 — 2026-07-19, this container)

| Lock | Status | Evidence |
|---|---|---|
| **1a** — ACH + corroboration gate at emission | **VERIFIED (local, release, hermetic)** | `holmes-core::analysis`: emission gate rejects any current finding with < 2 *independent* source roots (documented heuristic: distinct normalized roots — a deterministic floor, not proof of true independence), plus A-07 requirements (`knowability` assigned, non-empty `limits_of_this_finding`); non-empty provenance + confidence ∈ [0,1] already unrepresentable at `Finding` construction. Tests: `lock1a_uncorroborated_findings_cannot_leave_the_case`, `lock1a_corroborated_case_emits_and_hands_off`, emission unit suite. ACH: complete-matrix-or-refuse, fewest-inconsistencies verdict, tie-at-top reported honestly, diagnosticity computed |
| **1b** — full analytical case end-to-end on the smoke model | **COMPLETE (live, 2026-07-19)** | `holmes-case` (new bin) drove all six phases over one guarded ACP session (`anthropic`/`claude-sonnet-5`, MY_-prefixed credential seam): model supplied 4 hypotheses (la lluvia), 12/12 ACH cells, one likelihood pair, and an el-diablo challenge; deterministic side owned intake, fixtures, verdict, KAC, knowability (rule-assigned), emission, handoff. Result: verdict CASE COMPLETE exit 0, **64 frames** (F-030 correction: the earlier "54" was the first run's count; the committed evidence is the re-run on the F-026-fixed harness), egress 1/1 `api.anthropic.com:443 allowed`, ACH honestly reported a **3-way tie at top** (flag flowed to risk_flags), only E1 diagnostic. Born-redacted transcript: `docs/audit/evidence/1b-case-transcript-2026-07-19.json` |
| **1c** — blueprint-type CI check | **GREEN** | `no_blueprint_exports` structural test covers the whole `holmes-core` source tree including the new `analysis` module; passing in the workspace suite (CI step "Lock 0d") |
| **1d** — recipe safety scan live from the first recipe | **VERIFIED (local); CI steps landed** | First recipes committed (`recipes/la-lluvia.yaml`, `recipes/el-diablo.yaml`); `recipe-scan` bin (holmes-guard) detects zero-width/bidi/tag-block/variation-selector smuggling, fails closed, never auto-strips. Positive control: planted fixture (`planted_recipe_smuggled.yaml`, real U+200B/U+202E/tag-block payload spelling a hidden instruction) → 21 hits, exit 1. Negative: real recipes CLEAN exit 0; Spanish text does not false-positive. CI: both controls added to `acdl-gate.yml` |

**Phase 1 engines shipped (deterministic legs):** hypothesis objects (prior, predicted-present/absent, running log-likelihood with per-update trace), LR scorer (both conditionals required — prosecutor's fallacy unrepresentable; `CalibrationStatus::Uncalibrated` carried, gating → 2.5), ACH matrix, KAC (unexamined/failed always visible), first-principles quarantine (unprovenanced input has no type-level path into a rebuild), six-phase state machine (forward-only, typed prerequisites, intake can decline terminally). **Canon §5 firewall:** the brief's self-reported-certainty field is referenced nowhere in `analysis/` — enforced by structural regression test (which caught its own doc comment during the build; reworded per the F-025 precedent). Model-side legs live in `recipes/` and the `holmes-case` prompts.

## Phase 2 lock inventory (loop §6, Session 6 — 2026-07-19, this container) — The Wall

**Backend/architecture (D-09/D-12):** Graphiti **dropped** (F-027: base-deps `openai` + `posthog` — vendor + phone-home gates). The Wall is the *owned subset* in the new `crates/holmes-wall`, on **Neo4j Community Edition** (GPLv3) via **`neo4rs` 0.8.0** (MIT, denylist-clean tree — rider (d), verified via crates.io sparse index). No Graphiti, no `openai`, no phone-home; zero Python surface.

| Lock | Status | Evidence |
|---|---|---|
| **2a** — invalidation-not-deletion | **VERIFIED (contract, hermetic) + live leg env-gated** | Bi-temporal model (`graph.rs`): occurred-at/valid-until valid-time + ingested-at transaction-time; `supersede` sets `valid_until`+`invalidated_by` and appends — the `Wall` trait exposes **no delete/remove** capability. `InMemoryWall` reference store proves the contract (nothing deleted, superseded record preserved+flagged, `facts_as_of` returns history). Cypher builders (`cypher.rs`) asserted to carry **no DELETE/DETACH DELETE/REMOVE** (in-module test + `tests/no_delete_audit.rs`). **Live leg** (`Neo4jWall`, `tests/wall_locks::lock2a`) authored + env-gated on `HOLMES_NEO4J_URI`; **in-container Neo4j is org-egress-blocked** (Docker image blobs on `production.cloudfront.docker.com` → 403; `dist.neo4j.org` blocked) — same wall as 0c/0e; runs on any host with Neo4j |
| **2b** — AC-DL-1 §6 lands | **VERIFIED (local, release) — the CI §6 marker flips SCHEDULED→EXECUTING** | The deferral recorded in every CI run since Phase 0 is closed. `memory.rs`: the default (no-explicit-config) memory layer is **local-only** (loopback Ollama) — reaches **no cloud endpoint at all**; an excluded provider is unrepresentable (validated via L1b + the L1a allowlist). Live boundary (`tests/wall_locks::lock2b`): a forced `CONNECT` to the excluded canary → **HTTP 403 at L1a + a Denied egress event**. Structural inversion of Graphiti's excluded-vendor default. `acdl-gate.yml` §6 step now EXECUTING |
| **2c** — Tier-2 ingestion quality | **VERIFIED (scorer, hermetic) + live extraction run (Tier-1)** | Deterministic scorer (`ingest.rs`): grounded / ungrounded-citation / claim-exceeds-citation / malformed, as a **failure-rate report** — no model call in scoring; hermetic tests with good+bad fixtures. Live run (`holmes-ingest` bin) on the smoke model: **6/6 episodes grounded, 0 failures**, egress 1/1 `api.anthropic.com allowed`; transcript `docs/audit/evidence/2c-ingest-transcript-2026-07-19.json`. **Tier-2-on-Ollama** failure-rate numbers are environment-gated (Ollama model egress blocked in-container, established Phase 0); the scorer is identical for both tiers — carried |
| **2d** — supervised backend | **VERIFIED (local, hermetic)** | `supervise.rs`: `SupervisedBackend` with **kill-on-drop / no-orphan** (Drop kills+reaps), **path-confined data dir** (`..`-collapsed confinement check; dir outside root refused), **absolute program path required** (no system-install assumption). Proven deterministically with a stand-in child: spawn → drop → the PID is reaped (signal-0 probe); confinement + absolute-path refusals tested |
| **2e** — AC-WP weight provenance | **VERIFIED (local, hermetic)** | `provenance.rs`: `verify_weights` checks SHA-256 (+ optional size + optional required signature) **before load**, **failing closed** on any mismatch; "before load" is structural — a loader accepts only a `VerifiedWeights` token minted solely by a passing verify. Tests: valid verifies; **equal-length tamper → checksum fail-closed**; size mismatch, malformed digest, and required-signature-with-no-verifier all fail closed |

**Sweeps (release, `--locked`):** 101 tests pass (0 fail); fmt clean; clippy 0; **AC-DL-2 on the new `neo4rs` lock → CLEAN** (positive control still fires exit 1); recipe-scan CLEAN; A-01 0 hits; `openai`/`posthog`/`graphiti` **absent** from `Cargo.lock`, `neo4rs` present. **F-028**: the AC-DL-2 self-scan caught the new crate naming excluded vendors (enforcement code + comments) — resolved by the F-025 reword precedent (canary sourced from the exempted `policy.rs`), **zero new exemptions**.

**CVE gate on the `neo4rs` tree (D-13, 2026-07-19):** the supply-chain gate fired as designed on 4 **`informational: unmaintained`** RUSTSEC advisories (paste/backoff/instant/rustls-pemfile — no CVSS, no fix; verified primary). Martin ruled **allowlist exactly the 4** → `osv-scanner.toml` (each with rationale + `ignoreUntil = 2026-10-17` 90-day backstop; neo4rs 0.9 **stable** reopens the bump). CI now (a) passes `--config osv-scanner.toml` and (b) **asserts the suppression set is exactly those 4** — a 5th suppression fails the gate until a new D-item ratifies it. Gate stays **fail-closed for every scored/fixable advisory** (rider d). First CI run of these steps lands on the PR head.

## Adversarial self-verification (2026-07-18)

7 skeptics attacked each guard property; every reported defect re-reproduced against source before acceptance. 3 claims held (L2 env-strip, BYOK invariant, born-redacted output). 4 confirmed defects **fixed + regression-locked this session**: F-017 (MAJOR, L1a forward-proxy re-dispatch), F-018 (MAJOR, AC-DL-2 router exact-match evasion), F-019 (MINOR, mid-token excluded-family hole), F-020 (MINOR, §1 structural test fidelity). See findings-ledger.

## Environment (this container)

- Provider key: **ABSENT** (`ANTHROPIC_API_KEY` unset; only `ANTHROPIC_BASE_URL=https://api.anthropic.com`, keyless probe → 401; no Google key; D-05 residual)
- goose: **INSTALLED from source** — origin `aaif-goose/goose` @ `8e78960e535ab7f34630e7c5921a42f146cbc9f4` (Apache-2.0, verified on disk), v1.43.0, binary **`/home/user/goose-src/target/release/goose`** (trimmed: `--no-default-features --features rustls-tls` — no V8/code-mode, telemetry, aws-providers, system-keyring, updater). Container-ephemeral: rebuild per session or vendor.
- **Do not `cargo install goose-cli`** — the crates.io name is a squatter (F-016).
- Ollama: install blocked (ollama.com + GitHub release egress 403) — Tier-2 local smoke unavailable here.
- Siblings: `Alfred` / `wecanjustbuildthings.dev` **ABSENT from this container** (fresh clone, no symlinks). D-01 evidence already ledgered; no Alfred-touching work in scope.

## Cross-repo obligations — Alfred

| Obligation | Status |
|---|---|
| Artifact-level guard CI test (runs in Alfred's CI) | OPEN — [DIRECTIONAL] |
| OS/artifact-level egress enforcement (L1a residual: hostile binary ignoring proxy env) | OPEN — restated in `holmes-guard` docs + `docs/security.md` |
| Signed update channel with rollback | OPEN — [DIRECTIONAL] |
| Memory/resurfacing channel | OPEN — [DIRECTIONAL] |
| First-run rendering (tool-approval UX surface) | OPEN |
| `holmes-guard` adoption retiring `provider-lockdown.ts` | OPEN — adoption surface now real: `policy::PROVIDER_SELECTING_ENV_VARS`, `spawn::sanitized_spawn`, readable policy tables |
| Freshness note: `src/lib/provider-policy.test.ts` stale model-id fixtures | NOTED 2026-07-18 — cosmetic |

## Staging obligations — human

1. ~~**Fund the API account to finish 0c**~~ — **RESOLVED 2026-07-19**: human funded the account in-band; re-run produced exit 0, verdict ROUND-TRIP COMPLETE, streamed completion `"pong"` (transcript in `docs/audit/evidence/`). D-05 residual closed. Key-storage note stands: never store under `ANTHROPIC_API_KEY` in claude.ai/code env settings (reserved, auto-stripped); `MY_ANTHROPIC_API_KEY` is the working convention.
2. ~~**F-015**: supply AC doc v3~~ — **RESOLVED 2026-07-18**: v3 landed at root, normalized into `docs/acceptance/` (hash-gated), root removed, v2 to history; v3-conformance pass closed F-021…F-024.
3. ~~Mark the `acdl-gate` **and** `supply-chain` workflows **required status checks** on `main`~~ — **DONE 2026-07-19 (Martin).** `acdl-joint-gate` and `sbom-and-cve-scan` are both required status checks on `main`; both gates are now **binding, not advisory**. Completes AC-DL-2 c7's "required gate" clause and makes the 0e CVE gate binding.
4. Still-unlocated upstream artifacts (F-009/F-011): kickoff v2, `Iterative quality validation process.md`, `claude-code-epistemic-integration-prompt.md`.
5. DeepSeek alias primary re-verification (api-docs.deepseek.com egress) — carry to next open-egress session and Phase RC.

## Resume point

**Phase 2 build (Session 6):** locks **2a/2b/2c/2d/2e** green with executed evidence (see the Phase 2 inventory). Graphiti dropped (D-12/F-027); `holmes-wall` on Neo4j via `neo4rs`; **AC-DL-1 §6 landed** (CI marker flipped SCHEDULED→EXECUTING). D-09/D-10/D-11/D-12 ledgered; A-08 (spec Graphiti→owned-subset) recorded; upstream PR draft in `docs/upstream/`. **Two live legs carried, both blocked in-container by the same org egress wall as 0c/0e, never faked:** 2a's live-Neo4j leg (env-gated on `HOLMES_NEO4J_URI`; image/dist CDN 403) and 2c's Tier-2-Ollama failure-rate numbers (model egress blocked) — both run on any open-egress host. Carried forward: Phase 2.5 gating (CalibrationStatus + knowability-confidence); 0e provenance/attestation → RC. **Next after Phase 2 lands: Phase 2.5 — Safety before surface (HARD GATE)** then the Beta Scope Decision (a D-item for Martin). Phase 0 CLOSED (PRs #7–#9); Phase 1 landed (PR #10). Every fresh session: `git fetch origin` first; goose + Neo4j are container-ephemeral.
